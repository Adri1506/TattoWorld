[build]
builder = "dockerfile"
dockerfilePath = "outputs/production/Dockerfile"

[deploy]
startCommand = "nginx -g 'daemon off;'"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[environment]
NGINX_ENV = "production"
